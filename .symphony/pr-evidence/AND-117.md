# Plain-English summary
This PR adds a real `/greet` API contract, aligns Checker triggering with the v7 dual-review rules, and introduces a real GitHub release bundle plus acceptance helper for the sandbox `Released` and `Accepted` states.

## Execution-plan summary
- add `/greet` with `name` and `style` query handling
- keep `/health` unchanged
- document the endpoint contract in `README.md`
- publish a real GitHub release bundle from `main`
- verify the release bundle later with `scripts/accept_release.py`

## Contract-evidence summary
- API contract behavior is covered in `test_app.py`
- Checker trigger regression coverage is in `tests/test_checker_review.py`
- release bundle creation is covered in `tests/test_release_bundle.py`
- release acceptance behavior is covered in `tests/test_accept_release.py`

## Migration note
None

## Rollback note
None

## Plan deviation note
None

## Acceptance script
[STEP-01] Wait for the `Release` workflow on `main` to publish the latest GitHub release for the merge commit.
[STEP-02] Run `python3 scripts/accept_release.py --repo fpmam/myles-sandbox --tag <release-tag> --download-dir dist/acceptance`.
[STEP-03] Confirm the script prints `accepted release bundle ...` and exits `0`.
