# Plain-English summary
This PR keeps routine release and acceptance artifact output from dirtying the `myles-sandbox` repo.

## Execution-plan summary
- add the `AND-133` contract snapshot and execution plan
- ignore `dist/` so default local release and acceptance outputs stop showing up as untracked noise
- add a deterministic Git-based test that proves `dist` artifact paths are ignored while tracked repo sources are not

## Contract-evidence summary
- artifact ignore behavior is covered by `tests/test_artifact_hygiene.py`
- the issue package for this contract lives in `.symphony/contract-snapshot/AND-133.json` and `.symphony/execution-plan/AND-133.json`

## Migration note
None

## Rollback note
None

## Plan deviation note
None

## Acceptance script
[STEP-01] Run `./.venv/bin/python -m pytest -q tests/test_artifact_hygiene.py`.
[STEP-02] Run `git status --short --ignored dist` and confirm `dist/` is ignored (`!!`) rather than untracked (`??`).
[STEP-03] Run `./.venv/bin/python scripts/validate_plan_gate.py --repo-root . --issue-id AND-133`.
[STEP-04] After the PR number is known and the manifest is written, run `./.venv/bin/python scripts/validate_pr_gate.py --repo-root . --issue-id AND-133 --pr-number <PR_NUMBER> --repo fpmam/myles-sandbox`.
