# Plain-English summary
This PR hardens the hosted Referee and Checker workflow plumbing so reruns and multiple matching GitHub runs do not silently select stale success state.

## Execution-plan summary
- register a workflow subsystem for workflow-plumbing tickets
- add the `AND-132` contract snapshot and execution plan
- make `wait_for_check_runs.py` evaluate the latest check run per required name
- make `download_referee_artifact.py` choose the latest successful referee workflow run for the requested head SHA
- add deterministic tests for rerun and multi-match edge cases

## Contract-evidence summary
- rerun-aware check-run coverage lives in `tests/test_wait_for_check_runs.py`
- referee artifact selection coverage lives in `tests/test_download_referee_artifact.py`
- the issue package for this contract lives in `.symphony/contract-snapshot/AND-132.json` and `.symphony/execution-plan/AND-132.json`

## Migration note
None

## Rollback note
None

## Plan deviation note
None

## Acceptance script
[STEP-01] Run `./.venv/bin/python -m pytest -q`.
[STEP-02] Run `./.venv/bin/python scripts/validate_plan_gate.py --repo-root . --issue-id AND-132`.
[STEP-03] After the PR number is known and the manifest is written, run `./.venv/bin/python scripts/validate_pr_gate.py --repo-root . --issue-id AND-132 --pr-number <PR_NUMBER> --repo fpmam/myles-sandbox`.
